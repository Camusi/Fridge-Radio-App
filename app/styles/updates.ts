import { StyleSheet } from 'react-native';

export const updates = StyleSheet.create({
  errorContainer: {
    alignItems: 'center',
    marginBottom: 12,
  },
  errorText: {
    color: '#c00',
    textAlign: 'center',
    marginBottom: 8,
  },
  arrowButton: {
    padding: 8,
  },
  arrowButtonDisabled: {
    opacity: 0.3,
  },
  saveButton: {
    marginBottom: 16,
  },
  disabledButton: {
    opacity: 0.55,
  },
  actionButtons: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  iconButton: {
    padding: 8,
    marginLeft: 8,
  },
});
